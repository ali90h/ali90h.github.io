{
  "name": "ali90h.github.io",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "fetch": "node ./src/scripts/fetch-github.mjs",
    "build": "npm run fetch && vite build",
    "preview": "vite preview --outDir dist",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.0",
    "vitest": "^2.0.5"
  },
  "engines": { "node": ">=18" }
}
